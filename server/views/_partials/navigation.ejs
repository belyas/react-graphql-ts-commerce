
<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-fixed-top mb-3" role="navigation">
    <a class="navbar-brand" href="/">Node commerce</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobileNav" aria-controls="mobileNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mobileNav">
    <div class="navbar-nav">
        <a class="nav-item nav-link<%= currentPath === '' ? ' active' : '' %>" href="/">Home <span class="sr-only">(current)</span></a>
        <% if (isAuthenticated) {%>
            <a class="nav-item nav-link<%= currentPath === '/categories' ? ' active' : '' %>" href="/categories">Categories</a>
            <a class="nav-item nav-link<%= currentPath === '/products' ? ' active' : '' %>" href="/products">Products</a>
            <a class="nav-item nav-link<%= currentPath === '/profile' ? ' active' : '' %>" href="/profile">My Profile</a>
            <a class="nav-item nav-link pull-right" href="/auth/logout">Logout</a>
        <% } else {%>
            <a class="nav-item nav-link<%= currentPath === '/auth/login' ? ' active' : '' %>" href="/auth/login">Login</a>
        <% } %>
    </div>
    </div>
</nav>

<% if (errorMessage) {%>
    <div class="err-message"><%= errorMessage %></div>
<% } %>
<% if (successMessage) {%>
    <div class="success-message"><%= successMessage %></div>
<% } %>